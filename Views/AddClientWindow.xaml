<Window x:Class="Kursovaya.Views.AddClientWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление клиента"
        Height="420" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundGradient}">
    <Border CornerRadius="10"
            Background="White"
            BorderBrush="#2196F3"
            BorderThickness="2"
            Margin="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource ButtonGradient}"
                    CornerRadius="8,8,0,0"
                    Padding="20,15">
                <TextBlock Text="➕ ДОБАВЛЕНИЕ КЛИЕНТА"
                           FontSize="18"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"/>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="25,20,25,10">
                <StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="ФИО *" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,4"/>
                        <TextBox x:Name="FullNameBox" Height="36" FontSize="14" TextChanged="OnChanged"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Телефон" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,4"/>
                        <TextBox x:Name="PhoneBox" Height="36" FontSize="14" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Email" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,4"/>
                        <TextBox x:Name="EmailBox" Height="36" FontSize="14" TextChanged="OnChanged"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Примечание" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,4"/>
                        <TextBox x:Name="NotesBox" Height="72" FontSize="14" AcceptsReturn="True" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2"
                    Background="#F5F5F5"
                    CornerRadius="0,0,8,8"
                    Padding="16,12"
                    BorderBrush="#E0E0E0"
                    BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Отмена" Width="120" Height="36" Click="Cancel_Click" Background="#757575" Margin="0,0,8,0"/>
                    <Button x:Name="AddButton" Content="Добавить" Width="130" Height="36" Click="Add_Click" IsEnabled="False"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>