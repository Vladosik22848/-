<Window x:Class="Kursovaya.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему - Автосалон" 
        Height="671" Width="450" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundGradient}">
    <Border CornerRadius="10" 
            Background="White"
            BorderBrush="#2196F3"
            BorderThickness="2"
            Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" 
                    Background="{StaticResource ButtonGradient}"
                    CornerRadius="8,8,0,0"
                    Padding="20,20">
                <StackPanel>
                    <TextBlock Text="🚗 АВТОСАЛОН" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="White"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Информационно-справочная система" 
                               FontSize="14" 
                               Foreground="#E3F2FD"
                               HorizontalAlignment="Center"
                               Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="1" 
                        Margin="40,30,40,30"
                        VerticalAlignment="Center">
                <TextBlock Text="Вход в систему" 
                           FontSize="20" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextColor}"
                           Margin="0,0,0,30"
                           HorizontalAlignment="Center"/>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Email" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Margin="0,0,0,6"/>
                    <TextBox x:Name="LoginBox" 
                             Height="40"
                             FontSize="14"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,20">
                    <TextBlock Text="Пароль" 
                               FontSize="13" 
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Margin="0,0,0,6"/>
                    <PasswordBox x:Name="PasswordBox" 
                                 Height="40"
                                 FontSize="14"/>
                </StackPanel>


                <Button Content="Войти" 
                        Width="200" 
                        Height="45"
                        FontSize="16"
                        Click="Login_Click"
                        HorizontalAlignment="Center"
                        Margin="0,10,0,0"
                        x:Name="LoginButton"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
